// Iniciamos la variables
let suma = 0;
let contador = 1;

// Ciclo do while para sumar los numeros del 2 al 20
do {
    suma += contador;
    contador++;
} while (contador <= 10);

// Imprime el resultado 
console.log("La suma de los numeros del 1 al 10 es: " + suma);